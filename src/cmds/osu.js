let dp = require('despair')

module.exports = {
  name: 'osuprofile',
  aliases: ['osup'],
  description: 'Displays a osu! profile.',
  permissions: [],
  args: 1,
  usage: '<user>',
  exec: async (msg, cmd) => {
    // TODO
  }
}
