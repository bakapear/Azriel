let dp = require('despair')

module.exports = {
  name: 'anilist',
  aliases: ['ani'],
  description: 'Searches for an anime on anilist.',
  permissions: [],
  args: 1,
  usage: '<query> (poker)',
  exec: async (msg, cmd) => {
    // TODO
  }
}
